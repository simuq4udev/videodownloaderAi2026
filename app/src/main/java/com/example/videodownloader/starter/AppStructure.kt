package com.example.videodownloader.starter

/**
 * Entry point for starter architecture docs/constants.
 * Keep package names stable so future modules can be migrated incrementally.
 */
object AppStructure {
    const val APP_LAYER = "presentation"
    const val DOMAIN_LAYER = "domain"
    const val DATA_LAYER = "data"
}
